USE PD_311_DDL;
GO

--SELECT
--	[Ф.И.О.] = FORMATMESSAGE(N'%s %s %s', last_name,first_name,middle_name),
--	[Группа] = group_name,
--	[Напрвление] = direction_name
--FROM	Students 
-- JOIN  Groups ON ([group]=group_id)
--RIGHT JOIN Directions ON (Groups.direction = Directions.direction_id);
